<thead>
<tr>
  <% if table_called_from == 'permission_group' %>

    <% transcript_table_column_detail = FileTranscript.permission_group_columns.to_json %>
    <% JSON.parse(transcript_table_column_detail)['columns_status'].each do |_, value| %>
      <% if !value['status'].blank? && value['status'] == 'true' %>
        <th>
          <span style="max-width: 180px; width: 180px; float: left; min-width: 100px;"><%= display_field_title_table(value['value'],'transcript') %></span>
        </th>
      <% end %>
    <% end %>
  <% else %>
    <th>
      <input type="checkbox" class="select_all_checkbox_resources" data-url="<%= bulk_resource_list_collections_path %>"
             aria-label="Select all file transcripts" />
    </th>
    <% columns_details.each do |_k, h| %>
      <% if h['status'] == 'true' %>
        <th>
          <span style="max-width: 180px; width: 180px; float: left; min-width: 100px;"><%= FileTranscript.fields_values[h['value']] %></span>
        </th>
      <% end %>
    <% end %>
  <% end %>



  <th>Action</th>
</tr>
</thead>
<tbody>
</tbody>