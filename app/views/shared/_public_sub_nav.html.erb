<% if current_organization.present? %>
  <div id="sub_nav" class="<%= 'user_sub_nav' if organization_layout?  %>">
    <div class="container-fluid">
      <nav class="navbar navbar-expand-lg">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          Menu
        </button>
        <div class="collapse navbar-collapse " id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item" style="">
              <%= link_to root_url(subdomain: false), class: 'nav-link pb-0' do %>
                <%= image_tag 'homepage/main-logo.png', :class => "img_preview_cus_small" %>
              <% end %>
            </li>
            <li class="nav-item">
              <a href="javascript://" class="nav-link" data-toggle="modal" data-target="#signinmodal">Login</a>
            </li>
            <%= render 'shared/search_bar' %>
          </ul>
        </div>
      </nav>
      <%#= render 'shared/resource_layout_nav' %>
    </div>
  </div>
<% end %>