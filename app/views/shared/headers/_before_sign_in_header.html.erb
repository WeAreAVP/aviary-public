<!--Overriden: Changed-->
<header id="header">
  <div class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-light">
      <%= link_to image_tag("homepage/main-logo.png"), root_path, class: 'navbar-brand' %>

      <div class="search-nav search1cus">
        <% unless current_page?('/catalog') %>
          <div class="form-group">
            <div class="<%= container_classes %>">
              <%= render_search_bar %>
            </div>
          </div>
        <% end %>
      </div>
      <div class="nav-right text-right">
        <ul>
          <li>
            <%= link_to "Pricing", pricing_path %>
          </li>
          <li><a href="javascript://" data-toggle="modal" data-target="#signinmodal">Sign In</a></li>
          <li>
            <button class="btn btn-primary" data-toggle="modal" data-target="#signupmodal">Join Now</button>
          </li>
        </ul>
      </div>
    </nav>
  </div>
  <%= render 'shared/sub_nav' %>
</header>
<%= render 'shared/user_login_popup' %>
<%= render 'shared/user_registration_popup' %>
<%= render 'shared/forgot_password' %>
