development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://#{ENV.fetch("REDIS_HOST")}:6379/1" } %>
  channel_prefix: aviary_production
